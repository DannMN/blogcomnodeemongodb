<html>
<head>
{{!colocando titulo personalizado na pagina}}
{{#pageTitle}}
<title>{{pageTitle}}</title>
{{/pageTitle}}
{{!colocando titulo padrão caso o pesonalizado nao seja definido}}
{{^pageTitle}}
<title>{{h.defaultPageTitle}}</title>
{{/pageTitle}}
<style>
    
    *{
        
        box-sizing: border-box;
    }
    .container{
        margin: auto;
        max-width: 800px;
    }
    nav.menu ul {
        display: flex;
        justify-content: flex-end;
        list-style: none;
    }
    nav.menu ul a{
        padding: 1em;
        text-decoration: none;
        color: black;
        border: 1px;
        transition: border 0.2s;
    }
    nav.menu ul a:hover{
        border: 1px solid black;
        background-color: #c1c1c1;
        color: white;
    }
    .p-editable{
        background-color: #c1c1c1;
        padding:5px;
    }
</style>
</head>
<body>

<header>
    {{#h.pageTitle}}
        <h1>{{h.responseJson.pageTitle}}</h1>
    {{/h.responseJson.pageTitle}}
    {{^h.responseJson.pageTitle}}
        <h1>{{h.defaultPageTitle}}</h1>
    {{/h.responseJson.pageTitle}}
    <hr>
    <nav class="menu">
            <ul>
                {{!acessando os itens do menu que foi criado no helpers e saldo na 
                variavel global h}}
                {{#h.responseJson.menu}}
                <li><a href="{{slug}}">{{name}}</a></li>
                {{/h.responseJson.menu}}
            </ul>
    </nav>
    <hr>
</header>
<div class="container">
{{#flashes}}
    {{#success}}
    <div>{{.}}</div>
    {{/success}}
    {{#error}}
    <div>{{.}}</div>
    {{/error}}
{{/flashes}}